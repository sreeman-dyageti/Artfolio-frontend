const Navbar = () => {
    return (
        <header>
            <nav>
                <h1>Artfolio</h1>

                <div>
                    <a href="/">Home</a>
                    <a href="/login">Login</a>
                </div>
            </nav>
        </header>
    );
};

export default Navbar;